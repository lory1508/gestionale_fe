interface Travel {
  id: string;
  customerID: string;
  licensePlate: string;
  driverID: string;
  loadingPlace: string;
  loadingDate: string;
  unloadingPlace: string;
  unloadingDate: string;
  loadWeight: string;
  CMR: string;
  price: string;
  distance: number;
}

export default Travel;